# Banco de dados — gerado automaticamente ao subir a API
data/isyshell.db

# Cache do Python — gerado automaticamente, não tem utilidade no repositório
__pycache__/
*.pyc
*.pyo

# Configuração local do Claude Code — específica da máquina, não do projeto
.claude/

# Variáveis de ambiente — se criar futuramente para guardar tokens/segredos
.env
.env.*
